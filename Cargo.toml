[package]
# Metadata that is also used to generate the --help command:
name = "application"
version = "0.0.1"
authors = ["Daniel Schemmel"]
description = "A sample application"

# Other metadata
edition = "2018"
publish = false

[features]
default = []
bug = []
subcommands = []

[dependencies]
anyhow = "1"
flexi_logger = { version = "0.14", features = ["ziplogs"] }
log = "0.4"
structopt = "0.3"
thiserror = "1"

[dev-dependencies]
pretty_assertions = "0.6"
